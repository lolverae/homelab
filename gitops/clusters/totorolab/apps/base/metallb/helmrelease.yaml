apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: metallb
  namespace: metallb
spec:
  driftDetection:
    mode: enabled
    ignore:
      - paths: [""]
        target:
          kind: CustomResourceDefinition
  chart:
    spec:
      chart: metallb
      version: 0.16.1
      sourceRef:
        kind: HelmRepository
        name: metallb
        namespace: metallb
  interval: 1h
  install:
    crds: Create
  upgrade:
    crds: CreateReplace
